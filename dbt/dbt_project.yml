name: 'datastream_ai'
version: '1.0.0'
config-version: 2

profile: 'datastream_postgres'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  datastream_ai:
    staging:
      +materialized: view
    marts:
      +materialized: table
